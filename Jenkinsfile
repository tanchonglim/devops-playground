node {
    stage('Preparation') { // for display purposes
        // Get some code from a GitHub repository
        git 'https://github.com/tanchonglim/devops-playground.git'
    }
    stage('Build') {
        // run npm install
    }
    stage('Results') {
        echo 'success'
    }
}
